version: '3.8'

services:
  tglass:
    build: .
    container_name: tglass
    restart: unless-stopped
    ports:
      - "19944:80"
    volumes:
      - app:/app
      - app:/data
    environment:
      - PYTHONUNBUFFERED=1
      - TRADERNET_TICKER=TECHSMART