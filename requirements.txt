# Устанавливаем tradernet из локального файла
# Путь указывается относительно корня проекта
./tradernet_sdk-2.0.0.tar.gz

# Другие зависимости
requests>=2.31.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
maxapi>=0.1.0